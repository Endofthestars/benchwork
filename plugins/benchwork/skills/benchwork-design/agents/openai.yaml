interface:
  display_name: "Benchwork Design"
  short_description: "Design and preview a rigorous protocol"
  default_prompt: "Use $benchwork-design to draft a falsifiable study protocol."

dependencies:
  tools:
    - type: "mcp"
      value: "benchwork"
      description: "Benchwork scientific control plane"
