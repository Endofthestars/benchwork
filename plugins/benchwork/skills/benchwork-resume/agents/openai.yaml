interface:
  display_name: "Benchwork Resume"
  short_description: "Recover work from canonical research state"
  default_prompt: "Use $benchwork-resume to continue this study from canonical state."

dependencies:
  tools:
    - type: "mcp"
      value: "benchwork"
      description: "Benchwork scientific control plane"
